/**
 * Module: constants
 * Purpose: Centralize filesystem paths and configuration keys for graph memory storage.
 * Created: 2025-10-31
 * Skeleton by: OpenAI Codex (GPT-5)
 * Spec: specs/todo/graph-memory-core.md
 */

/**
 * Registry file path relative to the configured base directory.
 */
export const REGISTRY_FILE_PATH = '_system/registry.json';

/**
 * Ontology file path relative to the configured base directory.
 */
export const ONTOLOGY_FILE_PATH = '_system/ontology.yaml';

/**
 * Directory containing all node content files.
 */
export const NODE_CONTENT_DIRECTORY = '_content/nodes';

/**
 * Directory containing all connection content files.
 */
export const CONNECTION_CONTENT_DIRECTORY = '_content/connections';
